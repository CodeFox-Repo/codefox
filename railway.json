{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --frozen-lockfile --prod=false && pnpm --filter codefox-backend build && npm i -g --prefix /app/.pnpm-bin pnpm@9"
  },
  "deploy": {
    "startCommand": "PUPPETEER_EXECUTABLE_PATH=$(command -v chromium || command -v chromium-browser) node --experimental-specifier-resolution=node backend/dist/main",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
